# XDAG 矿工管理器-XDAG Miner

**项目概述**
- 基于 `Wails v2` + `Vue 3` 的桌面应用，用于管理 CPU 挖矿程序 `XMRig`
- 自带 `XMRig`官方原版 Windows 可执行文件（AMD64/ARM64）与默认配置，无需单独下载
- XMRig官方原版自带內置1%捐獻,这与本项目无关，若有需要后续看情况提供0%捐獻版本。
- 提供「控制面板 / 配置管理 / 运行日志」三大模块，支持一键启动/停止挖矿

**功能特点**
- 一键启动/停止挖矿，实时显示运行状态与算力
- 图形化管理矿池、钱包地址、CPU、HTTP API 等配置
- 自动生成并保存运行时配置到临时目录
- 内置日志面板，支持实时日志与清空操作

**运行环境**
- 系统：`Windows 10+`（当前嵌入的 `XMRig` 为 Windows 版）
- 工具：`Go 1.20+`、`Node 18+`、`Wails CLI`
- 权限：启用「大页内存」等优化可能需要管理员权限与安全软件允许

**快速开始**
- 诊断环境：`wails doctor`
- 开发运行：在项目根目录执行 `wails dev`
- 生产构建：在项目根目录执行 `wails build`

**使用流程**
- 进入「配置管理」页，填写矿池地址与钱包地址（必填）
- 根据需要调整 CPU 使用率、是否启用 HTTP API 等设置
- 点击「保存配置」后，返回「控制面板」启动挖矿
- 在「运行日志」页查看实时日志与状态事件

**重要提示**
- 默认矿池配置为 `stratum+ssl://equal.xdagminer.com:13003`，请将 `user` 替换为你的 `XDAG` 钱包地址
- 若「算力为 0」，请确认已启用 `HTTP API` 且端口未被占用（默认 `3649`）
- 某些安全软件可能拦截 `XMRig` 或其驱动（`WinRing0x64.sys`），请添加信任或白名单

**常见问题**
- 看不到算力：确认「配置管理」中启用 `HTTP API`，并确保端口（默认 `3649`）可访问
- CPU占用过高：降低「最大线程提示(%)」如 `50` 或更低
- 日志为空：确认已启动挖矿，或查看安全软件是否拦截
- 仅支持 Windows：当前内置 `XMRig` 为 Windows 可执行文件，其他系统需自行适配

**安全与合规**
- 挖矿需遵守当地法律法规；请勿在未经许可的设备上挖矿
- 启用性能优化（如大页内存）可能触发安全告警，请谨慎操作

**使用风险**：
- 本项目不对使用该项目过程中可能出现的任何损害或问题负责，使用者需自担风险。

**没有保证**：
- 本项目没有任何形式的明示或暗示的保证，包括但不限于适销性、特定用途适用性等。

**责任免除**：
- 项目的作者及贡献者不对任何因使用本项目而产生的直接、间接、附带、特殊或惩罚性损害承担责任。

**适用法律**：
- 本免责声明受 [适用国家或地区的法律] 管辖，若发生争议，双方同意通过 [选择的争议解决方式，如调解或仲裁] 解决。

**参考文件**
- 项目配置：`wails.json`
- 入口与绑定：`main.go`、`app.go`
- 服务与配置：`internal/service/xmrig_service.go`、`internal/service/config_service.go`
- 前端入口与页面：`frontend/src/main.js`、`frontend/src/App.vue`
- 如果你动手能力强：https://xmrig.com/

![img.png](/images%2Fimg.png)
![img_1.png](/images%2Fimg_1.png)
![img_2.png](/images%2Fimg_2.png)